[project]
name = "shared-schemas"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic[email]>=2.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
