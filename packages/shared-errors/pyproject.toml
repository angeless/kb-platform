[project]
name = "shared-errors"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
