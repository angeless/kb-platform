# 实施约束与不偏移开发规则

## 1. 目的
本文件用于防止开发过程把产品做偏，尤其避免把本产品错误实现成“普通文件转 Markdown 工具”或“只会总结的知识助手”。

## 2. 必须遵守的核心约束
### 2.1 不得删掉追溯关系
任何知识结论都必须可回到来源材料。

### 2.2 不得用单份总结替代知识系统
正式产物必须是多文档、多层级、可治理结构。

### 2.3 不得跳过知识系统架构
初次建库必须先形成架构初稿并允许用户审核。

### 2.4 不得直接覆盖旧知识
修正必须生成新版本，并说明原因。

### 2.5 不得吞掉冲突
冲突必须被显式记录，而不是悄悄合并。

### 2.6 不得忽略多租户隔离
所有数据和配置都必须按租户和项目隔离。

### 2.7 不得把模型配置写死
必须允许配置模型供应商、API Key、路由和预算。

## 3. 开发变更规则
- 修改核心业务流时必须同步更新对应文档；
- 修改状态流转时必须同步更新测试用例；
- 修改数据结构时必须更新接口文档与版本说明；
- 发现文档不完整时，优先补文档而不是自行假设。

## 4. AI 工程约束
- Prompt 不能要求模型忽略来源；
- Prompt 不能诱导模型擅自填补缺失事实；
- 模型输出必须经过校验层再入库；
- 复杂归类和架构识别必须保留理由摘要。

## 5. QA 阻断条件
若出现以下情况，QA 有权阻断发布：
- 无法生成架构图；
- 知识文档无来源；
- 冲突被隐性合并；
- 增量更新直接覆盖旧版本；
- 权限穿透；
- 模型密钥泄露风险。

## 6. 建议的交付物清单
开发阶段至少应同步维护：
- 版本记录；
- API 文档；
- 数据迁移脚本；
- 测试报告；
- 已知问题清单；
- 下一步计划。
