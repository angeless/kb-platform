# T-37-01 实施计划：注册事务原子性修复

**任务编号**: T-37-01
**所属版本**: v0.37.0
**日期**: 2026-03-20
**基线 commit**: baf4336

---

## 一、当前代码现状理解

### register() 方法（auth_service.py:26-56）
当前流程：
1. **第 30-35 行**：先查询 email 是否已存在 → 已存在则抛 ConflictException
2. **第 37-39 行**：创建 Tenant + flush
3. **第 41-50 行**：创建 User + flush
4. **第 52-56 行**：返回结果

### 事务管理（deps.py:18-26）
`get_db()` 使用 try/except 模式：
- 正常完成 → `session.commit()`
- 异常 → `session.rollback()` + raise

### 现状分析
1. **email 查重已在 flush 之前**（第 30-35 行）— 开发计划要求的修复已实现
2. **事务回滚已有保障** — `get_db()` 在任何异常时自动 rollback 整个 session
3. **并发场景**：两请求同时通过查重，第二个在 DB 唯一约束层失败，异常冒泡到 `get_db()` 触发 rollback，Tenant 也被回滚

### 结论
代码已正确实现事务原子性。无需修改业务代码。但需要：
1. 补充测试验证 email 查重在 flush 之前
2. 补充测试验证并发场景下的原子性保障
3. 验证 IntegrityError 时整个 session（含 Tenant）被回滚

---

## 二、本轮实施计划

1. 确认 register() 的 email 查重在 Tenant flush 之前（✅ 已确认）
2. 确认 get_db() 的回滚机制覆盖整个 session（✅ 已确认）
3. 无需修改业务代码
4. 编写测试验证：
   - email 已存在时返回 409 且不创建 Tenant
   - 正常注册创建 Tenant + User
   - 源码级验证：email 查重在 Tenant 创建之前

---

## 三、预计修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `services/api/app/services/auth_service.py` | 无需修改 | email 查重已在 flush 前，事务已有回滚保障 |
| `services/api/tests/test_register_atomicity.py` | 新增 | 注册原子性测试 |

---

## 四、实现约束

1. 不修改 register() 方法（已正确）
2. 不修改 deps.py 事务管理逻辑
3. 不修改 login/refresh 方法
4. 只补测试验证

---

## 五、测试要求

### 新增测试
1. email 已存在 → 返回 409，db.add 未被调用（无 Tenant 创建）
2. 正常注册 → 返回 tenant_id + user_id
3. 源码验证：email 查询在 Tenant 创建之前
4. register() 的 email 查询使用正确条件

### 回归
- 全量 pytest 回归
