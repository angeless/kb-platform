# v0.33.1 Phase 1 实施计划：基础准备

## 1. 当前代码现状

### 已有
- docker-compose.yml 配置了 7 个服务，但缺少健康检查和启动顺序控制
- Celery worker 代码存在但队列名与任务路由可能不一致
- Alembic 目录结构存在但无自动执行机制
- pipeline-worker 刚创建（v0.33.0），任务名为 `worker.tasks.run_pipeline`，需对齐为 `pipeline.run_pipeline`

### 问题
- I-01: docker-compose 缺失完整健康检查
- I-02: Celery 队列名不一致
- I-05: Alembic 迁移未自动执行

## 2. Phase 1 任务

### T-33-01: Docker Compose 全服务启动验证
- 补全 healthcheck 配置（postgres: pg_isready, redis: redis-cli ping）
- 添加 depends_on + condition: service_healthy
- 补全 pipeline-worker 完整配置

### T-33-02: Celery 任务队列名与路由对齐
- 统一任务名：`ingestion.parse_asset`, `pipeline.run_pipeline`, `orchestrator.*`
- 统一队列路由规则
- 对齐 job_service.py 的 send_task 调用

### T-33-05: Alembic 自动化
- 确保 API 服务启动时自动执行 `alembic upgrade head`
- 创建 entrypoint.sh 脚本

## 3. 预计修改文件

| 文件 | 操作 | 任务 |
|------|------|------|
| `docker-compose.yml` | 修改 | T-33-01 |
| `services/ingestion-worker/worker/celery_app.py` | 修改 | T-33-02 |
| `services/ingestion-worker/worker/tasks.py` | 修改 | T-33-02 |
| `services/pipeline-worker/worker/celery_app.py` | 修改 | T-33-02 |
| `services/pipeline-worker/worker/tasks.py` | 修改 | T-33-02 |
| `services/ai-orchestrator/` 任务文件 | 修改 | T-33-02 |
| `services/api/app/services/job_service.py` | 修改 | T-33-02 |
| `infra/docker/api-entrypoint.sh` | 新建 | T-33-05 |
| `infra/alembic/` | 检查/修改 | T-33-05 |

## 4. 约束
1. 不修改 API 签名
2. 不新增 DB 表
3. 不引入新依赖

## 5. 测试
- docker-compose up 全服务启动验证
- 回归：209+ tests 全部通过
