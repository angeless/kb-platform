# T-39-01 实施计划：修复 build.yml Dockerfile 路径

**任务编号**: T-39-01
**版本**: v0.39.0
**日期**: 2026-03-20
**作者**: Claude Code
**依赖**: 无

---

## 一、当前代码现状理解

### 1.1 build.yml 现状

- 触发条件：push tag `v*`
- 只定义了 **2 个** 构建 job（api 和 worker），缺少 pipeline-worker 和 ai-orchestrator
- Dockerfile 路径错误：
  - api: `services/api/Dockerfile`（不存在）→ 实际路径 `infra/docker/api.Dockerfile`
  - worker: `services/ingestion-worker/Dockerfile`（不存在）→ 实际路径 `infra/docker/ingestion-worker.Dockerfile`
- context 已正确设置为 `.`（项目根目录）

### 1.2 实际 Dockerfile 位置

- `infra/docker/api.Dockerfile`
- `infra/docker/ingestion-worker.Dockerfile`
- `infra/docker/pipeline-worker.Dockerfile`
- `infra/docker/ai-orchestrator.Dockerfile`

---

## 二、本轮实施计划

### 2.1 修正已有 2 个 job 的 Dockerfile 路径

- api: `file: services/api/Dockerfile` → `file: infra/docker/api.Dockerfile`
- worker: `file: services/ingestion-worker/Dockerfile` → `file: infra/docker/ingestion-worker.Dockerfile`

### 2.2 补齐缺失的 2 个 job

- 新增 pipeline-worker: `file: infra/docker/pipeline-worker.Dockerfile`
- 新增 ai-orchestrator: `file: infra/docker/ai-orchestrator.Dockerfile`

### 2.3 镜像命名规范

- api → `ghcr.io/<repo>/api:latest` + `:<version>`
- ingestion-worker → `ghcr.io/<repo>/ingestion-worker:latest` + `:<version>`
- pipeline-worker → `ghcr.io/<repo>/pipeline-worker:latest` + `:<version>`
- ai-orchestrator → `ghcr.io/<repo>/ai-orchestrator:latest` + `:<version>`

---

## 三、预计修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `.github/workflows/build.yml` | 修改 | 修正路径 + 补齐 4 个服务 |

**不修改**：
- 不修改 ci.yml
- 不修改 Dockerfile 内容
- 不修改 docker-compose.yml

---

## 四、实现约束

1. 只修改 build.yml，不改 Dockerfile 内容
2. context 保持为 `.`（项目根目录）
3. 保持现有 GHCR login、Buildx 配置不变
4. 镜像 tag 格式与已有 api/worker 保持一致

---

## 五、测试要求

| 验证项 | 方法 |
|--------|------|
| YAML 语法正确 | `python3 -c "import yaml; yaml.safe_load(open(...))"` |
| 4 个 Dockerfile 路径可达 | `test -f` 验证每个路径 |
| build.yml 引用 4 个正确路径 | grep 验证 |

### 回归验证
- 前端 vitest 回归（确认无影响）
- 后端单元测试回归（确认无影响）
