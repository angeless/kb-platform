# v0.29.0 前端批次 5：审核流程 — 实施计划

## 1. 当前代码现状理解

### 后端已有
- 单篇：review / publish / reject 端点
- 批量：batch/review / batch/publish / batch/reject
- 文档列表已有 status 字段筛选能力

### 前端已有
- 文档详情页已有单篇审核/发布按钮
- 缺少：审核队列页、批量操作面板、驳回弹窗

---

## 2. 本轮实施计划

### 交付页面/组件
1. **审核队列页** `/projects/[id]/review` — 筛选 reviewing 状态的文档，支持多选
2. **批量操作栏** — 选中多篇后显示批量通过/驳回按钮
3. **驳回弹窗组件** — 输入驳回原因

---

## 3. 预计创建文件清单

| 文件 | 操作 |
|------|------|
| `src/app/(dashboard)/projects/[id]/review/page.tsx` | 新建 — 审核队列 |
| `src/components/reject-modal.tsx` | 新建 — 驳回弹窗 |

---

## 4. 实现约束

1. 不修改后端代码
2. 批量操作调用已有 batch API
3. 驳回弹窗用原生 dialog 或 div overlay，不引入 UI 库

---

## 5. 测试要求

- `npm run build` 零错误
