# T-36-08 实施计划：修复冲突解决权限检查

**任务编号**: T-36-08
**所属版本**: v0.36.0
**日期**: 2026-03-20
**基线 commit**: 6dfa859

---

## 一、当前代码现状理解

### resolve_conflict 端点（conflicts.py:114-125）
- 已有 `current_user: User = require_role("reviewer")` 权限检查
- reviewer(2)、project_admin(3)、tenant_admin(4)、platform_admin(5) 可以调用
- viewer(0) 和 editor(1) 会被拒绝，返回 403

### 现状结论
**代码已正确实现**了 T-36-08 要求的权限检查。`require_role("reviewer")` 在 v0.12.0 RBAC 实施时已添加（参见 docs/reports/v0.12.0-rbac-report.md）。

### 缺失内容
- 没有纯单元测试验证 resolve 端点的角色权限（现有 test_conflicts.py 是需要 DB 的集成测试）
- 需要补充源码级验证测试，确认 require_role("reviewer") 确实存在

---

## 二、本轮实施计划

1. 确认 resolve 端点已有 `require_role("reviewer")`（✅ 已确认）
2. 无需修改代码
3. 补充源码级验证测试，确认权限检查存在

---

## 三、预计修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `services/api/app/routers/conflicts.py` | 无需修改 | 已有 require_role("reviewer") |
| `services/api/tests/test_conflict_resolve_rbac.py` | 新增 | 源码级权限验证测试 |

---

## 四、实现约束

1. 不修改 conflicts.py（已正确）
2. 不修改 conflict_service.py
3. 不修改冲突列表权限
4. 只补测试

---

## 五、测试要求

### 新增测试
1. 源码验证：resolve_conflict 端点使用 require_role("reviewer")
2. ROLE_HIERARCHY 验证：viewer < reviewer, editor < reviewer
3. ROLE_HIERARCHY 验证：reviewer >= reviewer, tenant_admin >= reviewer

### 回归
- 全量 pytest 回归
