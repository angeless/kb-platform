# 验收标准、自检与测试矩阵

## 1. 验收目标
确保开发团队在无人持续盯进度的情况下，仍然能按统一目标开发，并通过自检避免结果偏移。

## 2. 产品级验收标准
### 2.1 建库前架构可审核
上传样本资料后，系统必须先生成可视化架构图和结构化架构说明，并支持审核修改。

### 2.2 整理结果可追溯
任何知识文档中的关键结论，都必须能追溯到原始资料。

### 2.3 不发生知识坍缩
系统不得只输出单份高度压缩摘要代替知识库。

### 2.4 支持增量更新
新增资料进入后，系统应能识别其与旧知识的关系。

### 2.5 架构可治理
架构节点和版本必须可查看、可变更、可追溯。

## 3. 自检清单
开发完成一个模块后，至少自检以下问题：
- 是否保留了来源关系；
- 是否存在无解释名词；
- 是否有明确状态流转；
- 是否有失败处理；
- 是否能支持后续增量；
- 是否与架构节点绑定；
- 是否符合权限与审计要求。

## 4. 测试矩阵
### 4.1 接入测试
- 单文件上传；
- 批量上传；
- URL 导入；
- 压缩包导入；
- 非法格式拦截。

### 4.2 解析测试
- 图片 OCR；
- 音频转写；
- 视频抽帧与音轨处理；
- PDF 提取；
- 网页正文抓取。

### 4.3 架构测试
- 初版架构自动生成；
- 节点编辑；
- 版本发布；
- 节点绑定。

### 4.4 文档测试
- 主题文档生成；
- 术语表生成；
- 冲突文档生成；
- 来源索引完整性。

### 4.5 增量更新测试
- 新增主题；
- 补充旧文档；
- 修正旧文档；
- 生成冲突记录。

### 4.6 安全测试
- 越权访问；
- API Key 泄露；
- 恶意 URL 导入；
- 非法文件上传。

## 5. 回归要求
每次发布前至少回归：
- 建库主流程；
- 架构审核流程；
- 文档发布流程；
- 增量更新流程；
- 权限隔离；
- 模型配置。
