# v0.33.1 Phase 1: 基础准备 — 任务汇报

## 任务信息
- **版本**: v0.33.1
- **任务**: T-33-01 (Docker), T-33-02 (Celery 队列), T-33-03 (串联), T-33-05 (Alembic)
- **日期**: 2026-03-19

## 完成内容

### T-33-01: Docker Compose 全服务启动验证
- 所有 7 服务添加 healthcheck（postgres: pg_isready, redis: redis-cli ping, minio: curl /minio/health/live, api: curl /healthz）
- depends_on 改为 condition: service_healthy，确保启动顺序
- 新增 minio-init 容器：自动创建 kb-assets bucket
- 密钥支持环境变量覆盖：`${JWT_SECRET:-change-me-in-production}`

### T-33-02: Celery 队列名与路由对齐
- pipeline-worker 任务名设置为 `pipeline.run_pipeline`
- 所有 worker 统一 task_routes 配置
- docker-compose 中 worker 命令显式指定 `-Q ingestion` / `-Q pipeline` / `-Q ai`

### T-33-03: Ingestion → Pipeline 串联
- `parse_asset` 成功后自动创建 `kb_generate` Job 并调度 `pipeline.run_pipeline`
- 通过 `celery_app.send_task()` 指定 `queue="pipeline"`

### T-33-05: Alembic 自动化
- 新建 `api-entrypoint.sh`：等待 PG → 等待 Redis → `alembic upgrade head` → 启动 uvicorn
- API Dockerfile 安装 postgresql-client, redis-tools, curl, alembic
- 迁移文件 `infra/sql/alembic/` 复制到容器

### 新增文件
| 文件 | 说明 |
|------|------|
| `infra/docker/api-entrypoint.sh` | API 启动入口脚本（等待依赖+迁移+启动） |
| `infra/docker/pipeline-worker.Dockerfile` | Pipeline Worker 镜像定义 |

## 测试结果
| 测试组 | 通过 | 失败 |
|--------|------|------|
| API tests | 138 | 0 |
| AI Orchestrator | 40 | 0 |
| Ingestion Worker | 23 | 0 |
| Pipeline Worker | 8 | 0 |
| **总计** | **209** | **0** |
