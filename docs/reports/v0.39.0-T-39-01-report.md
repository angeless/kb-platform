# T-39-01 任务汇报：修复 build.yml Dockerfile 路径

**任务编号**: T-39-01
**版本**: v0.39.0
**日期**: 2026-03-20
**状态**: ✅ 已完成

---

## 1. 任务编号与目标

**T-39-01**: 修复 GitHub Actions build 工作流中 Dockerfile 路径错误，并补齐缺失的服务。

## 2. 文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `.github/workflows/build.yml` | 修改 | 修正 2 个路径 + 新增 2 个构建 step |
| `docs/plans/v0.39.0-T-39-01-plan.md` | 新增 | 实施计划文档 |

## 3. 数据 / API 影响

无。纯 CI/CD 配置变更。

## 4. 实现说明

### 路径修正
- `services/api/Dockerfile` → `infra/docker/api.Dockerfile`
- `services/ingestion-worker/Dockerfile` → `infra/docker/ingestion-worker.Dockerfile`

### 新增构建 step
- Pipeline Worker: `infra/docker/pipeline-worker.Dockerfile` → `ghcr.io/<repo>/pipeline-worker`
- AI Orchestrator: `infra/docker/ai-orchestrator.Dockerfile` → `ghcr.io/<repo>/ai-orchestrator`

### 镜像命名修正
- 原 worker 镜像名 `worker` → 修正为 `ingestion-worker`（与 Dockerfile 名一致）

## 5. 验证结果

| 验证项 | 结果 |
|--------|------|
| YAML 语法 | ✅ python3 yaml.safe_load 通过 |
| 4 个 Dockerfile 路径存在 | ✅ 全部存在 |
| build.yml 引用正确 | ✅ 4 个 `file:` 指向 `infra/docker/` |

## 6. 影响分析

- **构建修复**：push tag 后 GitHub Actions 将正确构建所有 4 个 Docker 镜像
- **向后兼容**：ingestion-worker 镜像名从 `worker` 变为 `ingestion-worker`，如有部署脚本引用旧名需同步更新
- **无代码变更**：不影响任何运行时行为

## 7. Commit

```
043e380 fix: correct Dockerfile paths in build.yml and add missing services (T-39-01, v0.39.0)
```

## 8. 是否继续

是。下一个任务为 **T-39-02: Docker 非 root 用户**。
