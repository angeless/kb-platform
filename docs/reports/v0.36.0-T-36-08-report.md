# T-36-08 任务汇报：修复冲突解决权限检查

---

## 任务编号
T-36-08

## 任务目标
只有 reviewer 及以上角色才能解决冲突。

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `services/api/app/routers/conflicts.py` | 无需修改 | 已有 `require_role("reviewer")`，代码正确 |
| `services/api/tests/test_conflict_resolve_rbac.py` | 新增 | 6 个源码级 RBAC 验证测试 |
| `docs/plans/v0.36.0-T-36-08-plan.md` | 新增 | 实施计划 |

## 数据 / API / 状态影响
- **API**: 无变更
- **行为变更**: 无（权限检查已在 v0.12.0 正确实现）
- **数据库**: 无变更
- **前端**: 无变更

## 实现说明

### 代码现状
`resolve_conflict` 端点（conflicts.py:119）已有：
```python
current_user: User = require_role("reviewer"),
```

`ROLE_HIERARCHY` 中 reviewer=2，viewer=0, editor=1 均低于 reviewer，会被 `require_role` 拒绝返回 403。

### 结论
代码已正确实现，无需修改。本任务仅补充测试验证。

### 测试策略
采用源码级验证（inspect.getsource）+ ROLE_HIERARCHY 层级验证，确保：
1. resolve_conflict 函数中确实引用了 `require_role("reviewer")`
2. viewer、editor 的层级低于 reviewer
3. reviewer、project_admin、tenant_admin 的层级 >= reviewer

## 验证结果
- **新增测试**: 6 个全部通过
  - 源码包含 require_role("reviewer") ✅
  - viewer < reviewer ✅
  - editor < reviewer ✅
  - reviewer >= reviewer ✅
  - tenant_admin >= reviewer ✅
  - project_admin >= reviewer ✅
- **回归测试**: 118 passed, 0 failed, 141 errors（DB 连接问题，已有基线）

## 影响分析
- **安全影响**: 确认权限检查已到位
- **回归风险**: 无（未修改代码）
- **衍生任务**: 无

## commit hash
15afca7

## 是否建议继续下一个任务
v0.36.0 全部 8 个任务已完成。建议进行版本收尾。
