# v0.10.0 架构节点待确认池 — 闭环汇报

## 1. 任务概述

| 项 | 值 |
|---|---|
| 版本 | v0.10.0 |
| 任务 | Sprint 3 — 架构节点待确认池 |
| 状态 | ✅ 完成 |
| 来源 | 09-开发任务拆分 Section 3.3 / doc 03 Section 8 |

## 2. 完成内容

### 2.1 Schema 增强
- 新增 `AssignNodeRequest` — 节点绑定请求体

### 2.2 文档待确认池
- `GET /v1/docs/pending?project_id=xxx` — 列出 node_id=NULL 的待确认文档
- `POST /v1/docs/{id}/assign-node` — 将文档绑定到指定架构节点
- `DocService.list_pending()` / `DocService.assign_node()` — 新增服务方法

### 2.3 冲突待确认池
- `GET /v1/conflicts/pending?project_id=xxx` — 列出 node_id=NULL 的待确认冲突
- `ConflictService.list_pending()` — 新增服务方法

### 2.4 Bug 修复
- `doc_service.py` / `conflict_service.py` — 添加 `from __future__ import annotations` 解决 `list` 方法名遮蔽 builtin `list` 导致的 TypeError

## 3. 修改文件清单

| 文件 | 操作 | 变更 |
|------|------|------|
| `packages/shared-schemas/shared_schemas/knowledge.py` | 修改 | +4 行 |
| `services/api/app/services/doc_service.py` | 修改 | +44 行 |
| `services/api/app/routers/docs.py` | 修改 | +30 行 |
| `services/api/app/services/conflict_service.py` | 修改 | +24 行 |
| `services/api/app/routers/conflicts.py` | 修改 | +17 行 |
| `services/api/tests/test_docs.py` | 修改 | +108 行 |

**合计**：6 个修改，+225 行

## 4. 测试结果

| 服务 | 测试数 | 结果 |
|------|--------|------|
| AI Orchestrator | 40 | ✅ 全部通过 |
| Ingestion Worker | 6 | ✅ 全部通过 |
| API unit tests | 14 | ✅ 全部通过 |
| API integration | 需 Docker | ⚠️ pre-existing |

## 5. API 新增端点

```
GET /v1/docs/pending?project_id=xxx
→ 列出所有 node_id=NULL 的文档（待确认池）

POST /v1/docs/{doc_id}/assign-node
Body: {"node_id": "uuid"}
→ 将文档绑定到指定架构节点

GET /v1/conflicts/pending?project_id=xxx
→ 列出所有 node_id=NULL 的冲突记录（待确认池）
```

## 6. Sprint 3 完成状态

| 任务 | 状态 | 版本 |
|------|------|------|
| 增量接入 API | ✅ | v0.8.0 |
| 旧知识匹配 | ✅ | v0.8.0 |
| 冲突记录与处理页 | ✅ | v0.1.0 |
| 文档版本比对 | ✅ | v0.9.0 |
| 架构节点待确认池 | ✅ | v0.10.0 |

**Sprint 3 全部完成。里程碑 M3（增量更新）已达成。**

## 7. 衍生任务记录（仅记录，不执行）

1. **批量绑定节点** — 当前只能逐个绑定，后续可支持批量操作
2. **自动建议节点** — LLM 建议最佳匹配节点供用户确认
