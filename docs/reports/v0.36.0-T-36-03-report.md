# T-36-03 任务汇报：修复 IDOR 跨租户节点分配

---

## 任务编号
T-36-03

## 任务目标
文档分配节点时验证目标节点属于同一项目的架构，防止 IDOR 跨租户/跨项目节点分配。

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `services/api/app/services/doc_service.py` | 修改 | `assign_node()` 查询 JOIN Architecture 表，增加 project_id 校验；import 新增 Architecture |
| `services/api/tests/test_assign_node_idor.py` | 新增 | 4 个测试覆盖 IDOR 防护 |
| `docs/plans/v0.36.0-T-36-03-plan.md` | 新增 | 实施计划 |

## 数据 / API / 状态影响
- **API**: 无变更（路径、参数、返回结构不变）
- **行为变更**:
  - 跨项目节点分配：之前成功 → 现在返回 404 `ARCH_NODE_NOT_FOUND`
  - 同项目节点分配：行为不变
- **数据库**: 无变更
- **前端**: 无变更

## 实现说明

### 修改前（doc_service.py:91）
```python
q = select(ArchitectureNode).where(ArchitectureNode.id == node_id)
```
只检查 node 是否存在，不检查项目归属。

### 修改后
```python
q = (
    select(ArchitectureNode)
    .join(Architecture, ArchitectureNode.architecture_id == Architecture.id)
    .where(
        ArchitectureNode.id == node_id,
        Architecture.project_id == doc.project_id,
    )
)
```
JOIN Architecture 表，确保节点所属架构与文档属于同一项目。

### 错误码
- 原：`ARCH_NOT_FOUND` → 现：`ARCH_NODE_NOT_FOUND`（更精确）
- 错误消息：`"节点不存在或不属于当前项目"`（不泄露其他项目节点信息）

## 验证结果
- **新增测试**: 4 个全部通过
  - `test_assign_node_same_project_succeeds` — 同项目分配成功
  - `test_assign_node_different_project_raises_404` — 跨项目分配返回 404
  - `test_assign_node_nonexistent_raises_404` — 不存在节点返回 404
  - `test_assign_node_query_joins_architecture` — 源码验证 JOIN 存在
- **回归测试**: 82 passed, 0 failed, 141 errors（DB 连接问题，已有）

## 影响分析
- **安全影响**: 修复 CRITICAL 级 IDOR 漏洞
- **回归风险**: 低 — 仅增加查询条件（JOIN + WHERE），不改函数签名
- **衍生任务**: 无

## commit hash
d1f3b4a

## 是否建议继续下一个任务
是 — 建议继续 T-36-04（修复文档发布状态校验）
