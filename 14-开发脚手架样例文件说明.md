# 开发脚手架样例文件说明

## 1. 目的
本文件说明随包提供的样例文件有什么作用，以及开发接手后应该如何修改。

## 2. 样例文件清单
- `manifest.json`：项目元信息、模块清单、运行入口说明。
- `.env.example`：环境变量模板，便于本地和测试环境初始化。
- `docker-compose.yml`：本地一键拉起依赖服务。
- `openapi/openapi.yaml`：对外接口草稿。
- `examples/sample_industry_kb/*`：示例行业知识库与知识系统架构样例。

## 3. 修改原则
### 3.1 可以修改的部分
- 服务名、镜像名、端口。
- 实际采用的数据库与消息队列配置。
- 具体的模型供应商配置。

### 3.2 不要删掉的部分
- 健康检查接口。
- 多租户上下文。
- 知识系统架构版本字段。
- 原始材料来源追踪字段。

## 4. 为什么要附样例文件
因为只有需求文档，开发容易理解偏；有了样例文件，开发能更快达成一致：
- 项目骨架长什么样；
- 接口如何命名；
- 配置如何传入；
- 示例知识库如何落地。
