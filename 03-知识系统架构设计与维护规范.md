# 知识系统架构设计与维护规范

## 1. 定义
“知识系统架构”是指用于描述整个知识库如何分库、分层、归类、扩展、更新和治理的上层结构文件集合。

它至少包含两种形态：
1. **可视化架构图**：给人看，便于审核与理解；
2. **结构化架构文件**：给系统和 AI 看，便于路由与维护。

## 2. 设计目标
- 让用户在正式建库前，就能审核整体分类是否正确；
- 让知识整理过程有固定归档依据；
- 让新材料进入时知道应该补到哪里；
- 让未来的 AI 维护者知道如何修改知识系统；
- 同时兼容“小型单库”与“大型多层级多库”。

## 3. 架构的两种形态
### 3.1 可视化架构图
推荐格式：SVG、PNG、Mermaid、可编辑脑图。

作用：
- 给用户审核初版结构；
- 给产品、运营、审核人员快速理解整体知识边界；
- 作为知识归类偏差的人工校对依据。

### 3.2 结构化架构文件
推荐格式：Markdown + JSON。

Markdown 用于说明规则，JSON 用于系统执行。

结构化文件至少包含以下字段：
- 架构 ID；
- 架构名称；
- 版本号；
- 行业域；
- 分库列表；
- 层级定义；
- 节点清单；
- 节点关系；
- 节点接收规则；
- 更新规则；
- 审核规则；
- 变更记录。

## 4. 架构中的核心概念
### 4.1 分库
分库是指逻辑上相对独立的一组知识集合。

示例：
- 行业通用知识库；
- 企业内部知识库；
- 项目专属知识库；
- 冲突知识库；
- 待审核知识池；
- 术语知识库。

### 4.2 层级
层级是指知识组织的纵向结构。

常见设计示例：
- L1：行业域；
- L2：业务模块；
- L3：知识主题；
- L4：知识文档；
- L5：来源证据。

并非所有场景都必须使用 5 层，但必须显式说明当前项目使用几层。

### 4.3 节点
节点是架构中的最小管理单元，可以是分类节点、主题节点、文档节点、术语节点、冲突节点、索引节点等。

## 5. 架构节点字段规范
每个节点必须定义：
- `node_id`：唯一标识；
- `node_name`：节点名称；
- `node_type`：节点类型；
- `parent_node_id`：父节点；
- `level`：所在层级；
- `description`：节点说明；
- `accept_types`：可接收的材料类型；
- `reject_types`：禁止接收的材料类型；
- `update_policy`：更新规则；
- `review_policy`：审核规则；
- `status`：草稿、待审核、已发布、已废弃。

## 6. 架构的生成时机
### 6.1 初次建库时
系统基于样本材料自动生成“初版架构”。

### 6.2 新行业进入时
若现有知识系统与新材料所在行业差异较大，应生成新的架构分支或新的知识库。

### 6.3 持续更新时
若频繁出现无法归类的资料，说明原架构不再适配，需要迭代架构版本。

## 7. 架构审核流程
1. 系统生成初版架构图与结构化说明；
2. 用户审核一级分类、子分类、层级数量、分库方式；
3. 人工调整节点；
4. 生成架构 v1；
5. 之后的任何结构性改动都必须走版本化流程。

## 8. 架构与知识内容的绑定规则
- 每份知识文档必须绑定一个架构节点；
- 每份原始资料至少要绑定一个来源节点；
- 每次增量更新必须记录它修改的是哪个节点；
- 若无法绑定，则进入“待确认节点池”；
- 若与旧知识冲突，则进入“冲突节点池”。

## 9. 增量维护规则
### 9.1 允许的变更
- 新增节点；
- 调整节点说明；
- 在不破坏旧绑定的前提下新增子节点；
- 废弃节点但保留历史引用。

### 9.2 不允许的变更
- 直接删除已发布节点；
- 无版本记录地重排大层级；
- 在无审核情况下改变节点接收规则；
- 用新的总结直接覆盖旧证据。

## 10. 结构化架构文件示例
```json
{
  "architecture_id": "ksa_demo_001",
  "name": "客户服务知识系统",
  "version": "1.0.0",
  "levels": [
    {"level": 1, "name": "业务域"},
    {"level": 2, "name": "知识模块"},
    {"level": 3, "name": "主题"},
    {"level": 4, "name": "文档"},
    {"level": 5, "name": "来源"}
  ]
}
```

## 11. 推荐产出文件
- `knowledge-system-architecture.md`
- `knowledge-system-architecture.json`
- `knowledge-system-architecture.mmd`
- `knowledge-system-architecture.svg`

## 12. 对开发的要求
开发必须将“知识系统架构管理”作为独立模块实现，不能只把它写死在文档模板里。
